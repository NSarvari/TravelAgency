@model IEnumerable<TravelAgency.Models.Customer>

<h2>Customers</h2>

<p>
    <a asp-action="Create" asp-controller="Customer" class="btn btn-success">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Phone Number</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model)
        {
            <tr>
                <td>@customer.Name</td>
                <th>@customer.PhoneNumber</th>
                <td>
                    <a asp-action="Details" asp-route-id="@customer.Id" class="btn btn-secondary">Details</a>
                    <a asp-action="Update" asp-route-id="@customer.Id" class="btn btn-primary">Update</a>
                    <form asp-action="Delete" asp-route-id="@customer.Id" method="post"
                          onsubmit="return confirm('Are you sure you want to delete this customer?');">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>